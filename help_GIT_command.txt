Daily - RUNNING
---------------------------------------------------------------
##Cloning a repo
git clone http://github.com/symfony/symfony.git
---------------------------------------------------------------
##push a file to repo
git add .
git status
git commit -m 'message' 
git push origin master
#name of the repot: origin and name of the branch: masters


1 Time - INITIALIZE
---------------------------------------------------------------
##installer git sur LINUX
sudo apt-get install git-core gitk

---------------------------------------------------------------
##initialiser un dépot sur ordi local dans le dossier concerné
## un dossier .git/ sera alors créé: pour le supprimer : rm -rf .git/
git init

---------------------------------------------------------------
##créer le repo sur Github et récuperer le lien
git remote add origin https://github.com/kevinzous/AzimuthsLearning.git
git push -u origin master

##----------------------------------
config the password
git config --global credential.helper store
git pull
##input user pwd
##if changed need to remove it here ~/.git-credentials
---------------------------------------------------------------
git clone http://github.com/symfony/symfony.git
---------------------------------------------------------------
##push a file to repo
git add .
git status
git commit -m 'message' 
git push origin master
#name of the repot: origin and name of the branch: masters
